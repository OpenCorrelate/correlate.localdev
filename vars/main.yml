---
# vars file for correlate.localdev


# correlate.homebrew vars

homebrew_installed_packages:
  - ssh-copy-id
  - python
  - zsh
  - keychain
  - ansible
  - packer

homebrew_upgrade_all_packages: no

homebrew_taps:
  - caskroom/cask

homebrew_cask_apps:
  - iterm2
  - virtualbox
  - virtualbox-extension-pack
  - vagrant


homebrew_cask_appdir: /Applications